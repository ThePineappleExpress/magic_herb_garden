[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "crypto_rs"
version = "0.1.0"
description = "Rust-backed AES-256-GCM + PBKDF2 crypto primitives for Magic Herb Tracker"
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
