[project]
name = "magic-herb-tracker"
version = "0.1.0"
description = "A grow tracking app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kivy>=2.3.1",
    "kivy-garden>=0.1.5",
]

[tool.uv.workspace]
members = [
    "magic_herb_tracker",
]
